(function () {
    'use strict';
    var ejControllers = angular.module('ej.ejControllers', []);

    //城市选择
    ejControllers.controller('indexController', ['$window', '$rootScope', '$scope', '$location',
        function ($window, $rootScope, $scope, $location) {
            //cityCataService.getcities().success(function (data) {
            //    $scope.newCities = data;
            //});

        }]);

    ejControllers.controller('index2Controller', ['$window', '$rootScope', '$scope', '$location',
        function ($window, $rootScope, $scope, $location) {

        }]);
})();